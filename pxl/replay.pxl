import px

df = px.DataFrame(table='http_events', start_time='-5m')
df = df[px.contains(df.ctx['pod'], 'image-converter')]

# Restrict number of results.
df = df.head(10)

# Order columns.
df = df[['req_body']]

px.display(df)